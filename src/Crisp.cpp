#include "Crisp.h"

Crisp::Crisp(string name, float a, float b, float alpha, float beta)
{
    this->name = name;
    this->a = a;
    this->b = b;
    this->alpha = alpha;
    this->beta = beta;
}

Crisp::Crisp(string name, string values)
{
    float a, b, alpha, beta;

    values.reeeee


    Crisp(name, a, b, alpha, beta);
}
